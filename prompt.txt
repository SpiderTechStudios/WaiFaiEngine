Modify ONLY the testing WiFiDog controller, `TestWifiDogController`. Do not change production controllers, services, models, migrations, database schema, routes outside what is strictly required for the test, or existing production authentication/payment logic.

The purpose of this test is to verify the actual Ruijie RAP62-OD WiFiDog flow step-by-step. Payment is NOT part of this test.

Current hardware/request example:

* Gateway ID: `58b4bb192d35`
* Gateway SN: `G1UQ5C8006474`
* Gateway LAN IP: `192.168.0.144`
* Gateway WiFiDog port: `2060`
* Client IP: `192.168.0.60`
* Client MAC: `9e:d3:c4:aa:49:88`
* SSID: `500KwaMasaa24Gift`
* Original URL: `http://connectivitycheck.gstatic.com/generate_204`

CURRENT BAD FLOW:

`/wifidog/login`
→ Laravel generates/resolves token
→ Laravel immediately redirects to `http://192.168.0.144:2060/wifidog/auth?...`
→ Ruijie returns/calls `/wifidog/portal`
→ portal receives `message=denied`

We do NOT want that flow for this test.

IMPLEMENT THIS EXACT TEST FLOW:

1. `/wifidog/login`

When Ruijie calls `/wifidog/login`:

* Log the complete request.
* Read:

  * `gw_id`
  * `gw_sn`
  * `gw_address`
  * `gw_port`
  * `ip`
  * `mac`
  * `ssid`
  * `url`
* Resolve the active gateway using the existing `GatewayResolver`.
* Resolve/create the captive session using the existing `CaptiveSessionService::resolveOrCreate()`.
* Preserve the original requested URL.
* Get the application's captive-session token.
* DO NOT redirect to the gateway `/wifidog/auth` from `/login`.
* DO NOT attempt to authorize the client from `/login`.
* Instead redirect the browser to our Laravel testing portal:

`/api/wifidog/portal`

Pass the session token and relevant gateway/client information in the query string.

Log something like:

`simulator.login_redirect_portal`

with:

* gw_id
* gw_address
* gw_port
* client MAC
* client IP
* original URL
* SHA-256 hash of token
* redirect URL with the token masked

2. `/wifidog/portal`

This endpoint represents the actual captive portal.

When reached:

* Log the complete request/query.
* Read the captive-session token.
* Find the session using `CaptiveSessionService::findByToken()`.
* If token is missing, return HTTP 422 JSON.
* If session does not exist, return HTTP 404 JSON.
* DO NOT redirect to Google or the original URL.
* DO NOT automatically authorize the client.
* DO NOT return `message=denied`.
* Return a very simple HTML test page saying:

  `WaiFai Test Portal`

  `Portal successfully reached.`

Show:

* client IP
* client MAC
* session ID

And provide one link/button:

`ACCEPT TEST CLIENT`

The link must point to:

`/api/wifidog/portal/accept?token=...`

This lets us manually trigger the next stage.

3. Add `/wifidog/portal/accept`

Add a testing-only method to `TestWifiDogController` called `accept()`.

When this endpoint is called:

* Log the request/query.
* Read the token.
* Find the captive session with `findByToken()`.
* Return 422 if token is missing.
* Return 404 if the session cannot be found.

Then authenticate the application's captive session using the EXISTING service method:

`CaptiveSessionService::authenticate()`

Use the session's existing client MAC/IP.

IMPORTANT:
This application-level authentication is only for testing the application state. Do not claim this proves that Ruijie has authorized the client.

After the application session is authenticated, construct the Ruijie gateway's local WiFiDog auth URL:

`http://{gw_address}:{gw_port}/wifidog/auth`

For the FIRST test, pass the application's captive-session token as:

`?token={token}`

Then redirect the browser to that gateway URL.

Log:

`WIFIDOG PORTAL -> GATEWAY AUTH`

including:

* gateway address
* gateway port
* session ID
* client MAC
* client IP
* SHA-256 token hash
* redirect URL with token masked

4. `/wifidog/auth`

Keep the existing endpoint, but make it purely diagnostic for this test.

When called:

* Log the complete request/query.
* Log useful headers such as User-Agent and Referer.
* Return exactly:

`Auth: 1`

with HTTP 200 and `Content-Type: text/plain`.

DO NOT automatically modify gateway state here unless the existing implementation already has a legitimate service method specifically intended for this callback.

5. `/wifidog/ping`

Keep the existing diagnostic behavior:

* Log the request.
* Return:

`Pong`

6. DO NOT change the existing `test()` method unless absolutely necessary.

The existing `test()` method is an application-level end-to-end simulation and should remain separate from the real browser/gateway test.

7. Add/update routes ONLY if needed for the new testing endpoint:

`GET /api/wifidog/login`
`GET /api/wifidog/portal`
`GET /api/wifidog/portal/accept`
`GET /api/wifidog/auth`
`GET /api/wifidog/ping`

Make sure `/portal/accept` is reachable.

IMPORTANT ARCHITECTURE RULES:

* `/login` must NOT directly redirect to the gateway `/wifidog/auth`.
* `/login` → `/portal` first.
* `/portal` must NOT redirect to Google.
* `/portal` must NOT automatically approve the client.
* `/portal/accept` is where the test user explicitly triggers authentication.
* `/portal/accept` then redirects to the gateway's local `/wifidog/auth`.
* We want to observe exactly what Ruijie does after that.
* Do not invent a `message=accepted` response.
* Do not assume that returning JSON from `/portal` authorizes the WiFi client.
* Do not implement firewall rules or fake an `online=true` state.
* Do not add payment logic.
* Do not modify the real payment/access-grant workflow.

EXPECTED TEST TRACE:

The logs should become:

`call from Login`
→ `simulator.login_redirect_portal`
→ `WIFIDOG PORTAL`
→ browser displays `WaiFai Test Portal`
→ user clicks `ACCEPT TEST CLIENT`
→ `WIFIDOG PORTAL ACCEPT`
→ `WIFIDOG SESSION AUTHENTICATED`
→ `WIFIDOG PORTAL -> GATEWAY AUTH`
→ browser goes to `192.168.0.144:2060/wifidog/auth`
→ observe what Ruijie does next.

VERY IMPORTANT:

Do not simply rewrite the controller from scratch and remove existing functionality. Inspect the current `TestWifiDogController`, existing `CaptiveSessionService`, `GatewayResolver`, and route definitions first. Reuse existing method/property names and adapt to the actual models/services in this repository.

Before editing, inspect the signatures and return values of:

* `CaptiveSessionService::resolveOrCreate()`
* `CaptiveSessionService::findByToken()`
* `CaptiveSessionService::authenticate()`
* `CaptiveSessionService::authorizeToken()`
* `CaptiveSessionService::gatewayAuthRedirectUrl()`
* `GatewayResolver::resolveActive()`

Do not guess their APIs.

After making the changes, run the relevant PHP syntax/tests if available and report exactly which files were changed.

Do not modify production behavior outside the testing WiFiDog controller/routes required for this test.
